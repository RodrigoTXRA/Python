# container where we can store multiple data
# Tuple data cannot be changed
coordinates = (4, 5)
# This code below would change the tuple but python does not allow
# Is used for data which will never change such as coordinates
# Are used in special situation only for data which cannot be changed
print(coordinates[0])
